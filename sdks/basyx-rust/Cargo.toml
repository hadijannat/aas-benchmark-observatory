[package]
name = "aas-benchmark-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
basyx-rs = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "pipeline"
harness = false
